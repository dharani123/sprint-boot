export const GROUPS = [
  { id: 'hello', label: 'Hello APIs', items: [
    { key: 'hello-hello',   method: 'GET', label: '/hello' },
    { key: 'hello-greet',   method: 'GET', label: '/greet/{name}' },
    { key: 'hello-welcome', method: 'GET', label: '/welcome' },
  ]},
  { id: 'actuator', label: 'Actuator (Dashboard)', items: [
    { key: 'act-all',      method: 'GET', label: '/actuator' },
    { key: 'act-health',   method: 'GET', label: '/actuator/health' },
    { key: 'act-info',     method: 'GET', label: '/actuator/info' },
    { key: 'act-metrics',  method: 'GET', label: '/actuator/metrics' },
    { key: 'act-beans',    method: 'GET', label: '/actuator/beans' },
    { key: 'act-mappings', method: 'GET', label: '/actuator/mappings' },
    { key: 'act-env',      method: 'GET', label: '/actuator/env' },
  ]},
  { id: 'profiles', label: 'Profiles & Config', items: [
    { key: 'profile-info',  method: 'GET', label: '/api/info' },
    { key: 'profile-value', method: 'GET', label: '@Value injection' },
  ]},
  { id: 'validation', label: 'Bean Validation', items: [
    { key: 'val-allbad',     method: 'POST', label: 'All Fields Invalid' },
    { key: 'val-partialbad', method: 'POST', label: 'Email Invalid Only' },
    { key: 'val-toolong',    method: 'POST', label: 'Name Too Long' },
    { key: 'val-valid',      method: 'POST', label: 'Valid Request ✓' },
  ]},
  { id: 'exceptions', label: 'Exception Handling', items: [
    { key: 'ex-notfound',  method: 'GET',  label: '404 Not Found' },
    { key: 'ex-duplicate', method: 'POST', label: '409 Duplicate Email' },
    { key: 'ex-badtype',   method: 'GET',  label: '400 Bad Type' },
    { key: 'ex-badjson',   method: 'POST', label: '400 Bad JSON' },
  ]},
  { id: 'students', label: 'Students API (JPA)', items: [
    { key: 'students-getAll',   method: 'GET',    label: '/api/students' },
    { key: 'students-getOne',   method: 'GET',    label: '/api/students/{id}' },
    { key: 'students-create',   method: 'POST',   label: '/api/students' },
    { key: 'students-update',   method: 'PUT',    label: '/api/students/{id}' },
    { key: 'students-delete',   method: 'DELETE', label: '/api/students/{id}' },
    { key: 'students-byCourse', method: 'GET',    label: '/course/{course}' },
    { key: 'students-search',   method: 'GET',    label: '/search?keyword' },
  ]},
  { id: 'dtos', label: 'DTOs — Concept 14', items: [
    { key: 'dto-getall',     method: 'GET',  label: 'GET all (displayLabel)' },
    { key: 'dto-create',     method: 'POST', label: 'POST (no id field)' },
    { key: 'dto-id-ignored', method: 'POST', label: 'POST with id: 99 (ignored)' },
  ]},
  { id: 'pagination', label: 'Pagination & Sorting (C13)', items: [
    { key: 'page-default',    method: 'GET', label: '/paged (default)' },
    { key: 'page-2',          method: 'GET', label: '/paged page=1, size=2' },
    { key: 'page-sortname',   method: 'GET', label: '/paged sort by name' },
    { key: 'page-sortcourse', method: 'GET', label: '/paged sort by course desc' },
  ]},
  { id: 'transactions', label: '@Transactional (Concept 12)', items: [
    { key: 'tx-count',         method: 'GET',  label: '/api/students/count' },
    { key: 'tx-bulk-success',  method: 'POST', label: 'Bulk Enroll (success)' },
    { key: 'tx-bulk-rollback', method: 'POST', label: 'Bulk Enroll (rollback)' },
  ]},
  { id: 'orders', label: 'Orders API (DI)', items: [
    { key: 'orders-summary', method: 'GET',  label: '/api/orders/summary' },
    { key: 'orders-getAll',  method: 'GET',  label: '/api/orders' },
    { key: 'orders-place',   method: 'POST', label: '/api/orders' },
  ]},
  { id: 'products', label: 'Products API', items: [
    { key: 'products-getAll',  method: 'GET',    label: '/api/products' },
    { key: 'products-getOne',  method: 'GET',    label: '/api/products/{id}' },
    { key: 'products-create',  method: 'POST',   label: '/api/products' },
    { key: 'products-update',  method: 'PUT',    label: '/api/products/{id}' },
    { key: 'products-delete',  method: 'DELETE', label: '/api/products/{id}' },
  ]},
]

export const ENDPOINTS = {
  'hello-hello':   { title: 'GET /hello', desc: '@GetMapping — simplest possible endpoint', analogy: '🍽️ The simplest waiter request — you walk in and ask "hello?", the restaurant just replies back.', concept: '<strong>@GetMapping</strong> maps a <code>GET</code> HTTP request to a Java method. When someone visits <code>/hello</code>, Spring calls <code>sayHello()</code> and sends back whatever it returns.', method: 'GET', url: '/hello', params: [], headers: [], body: '' },
  'hello-greet':   { title: 'GET /greet/{name}', desc: '@PathVariable — extract a value embedded in the URL path', analogy: '📋 Like a hotel receptionist reading the name off your reservation card — the name is part of the request path itself.', concept: '<strong>@PathVariable</strong> pulls a value out of the URL path. The <code>{name}</code> in <code>@GetMapping("/greet/{name}")</code> is a placeholder. Whatever you put there gets injected into the method parameter.', method: 'GET', url: '/greet/Dharani', params: [], headers: [], body: '' },
  'hello-welcome': { title: 'GET /welcome?city=X', desc: '@RequestParam — read a value from the query string (?key=value)', analogy: '📝 Like a preference form — "Which city are you from?" is optional. If you skip it, a default answer is used.', concept: '<strong>@RequestParam</strong> reads from the query string (<code>?city=Chennai</code>). The <code>defaultValue</code> means the param is optional — if missing, the default kicks in.', method: 'GET', url: '/welcome', params: [{key:'city',value:'Chennai',enabled:true}], headers: [], body: '' },
  'act-all':       { title: 'GET /actuator', desc: 'Lists every actuator endpoint enabled in the current profile', analogy: '🚗 Opening the car dashboard for the first time — it shows you every gauge and dial available to you.', concept: '<strong>Spring Boot Actuator</strong> adds production-ready monitoring with one dependency. This root endpoint lists all available sub-endpoints. In dev, all are exposed. In prod, only <code>health</code> and <code>info</code> — never expose <code>beans</code> or <code>env</code> in production.', method: 'GET', url: '/actuator', params: [], headers: [], body: '' },
  'act-health':    { title: 'GET /actuator/health', desc: 'Shows overall health + each component (DB, disk, custom indicators)', analogy: '❤️ A doctor\'s full body checkup report. Overall status = UP/DOWN. Each organ (DB, disk) gets its own result.', concept: '<strong>Built-in health indicators</strong>: Spring auto-adds <code>db</code> (can it reach H2?), <code>diskSpace</code> (is disk full?). <strong>Custom indicator</strong>: our <code>StudentDbHealthIndicator</code> is picked up automatically and added as <code>studentDb</code>.', method: 'GET', url: '/actuator/health', params: [], headers: [], body: '' },
  'act-info':      { title: 'GET /actuator/info', desc: 'Shows custom info defined in application-dev.properties under info.*', analogy: '📋 The app\'s ID card — name, version, description.', concept: '<strong>info.*</strong> properties in application-dev.properties are automatically exposed here. CI/CD pipelines often inject the git commit hash and build timestamp so you can tell exactly what version is running.', method: 'GET', url: '/actuator/info', params: [], headers: [], body: '' },
  'act-metrics':   { title: 'GET /actuator/metrics', desc: 'Lists all available metrics — JVM memory, CPU, HTTP request counts', analogy: '📊 The car\'s instrument cluster — speed (request rate), fuel (memory), engine temperature (CPU).', concept: '<strong>Built-in metrics</strong>: <code>jvm.memory.used</code>, <code>http.server.requests</code>, <code>hikaricp.connections</code>. Drill into any metric by appending its name: <code>/actuator/metrics/jvm.memory.used</code>.', method: 'GET', url: '/actuator/metrics', params: [], headers: [], body: '' },
  'act-beans':     { title: 'GET /actuator/beans', desc: 'Lists every bean Spring created in the ApplicationContext', analogy: '🗂️ The complete staff directory of your company. Every employee (bean), their job title (type), and who they report to (dependencies).', concept: '<strong>This is the IoC Container made visible.</strong> You\'ll see every bean we created (StudentController, StudentService, StudentRepository...) plus hundreds Spring created automatically (Tomcat, Jackson, Hibernate, HikariCP...).', method: 'GET', url: '/actuator/beans', params: [], headers: [], body: '' },
  'act-mappings':  { title: 'GET /actuator/mappings', desc: 'Lists every @GetMapping, @PostMapping etc. registered in all controllers', analogy: '🗺️ The complete route map of your city (app). Every road (URL) and where it leads.', concept: '<strong>DispatcherServlet route registry made visible.</strong> You\'ll see all mappings Spring registered at startup. Each entry shows: the URL pattern, HTTP method, controller class, and method.', method: 'GET', url: '/actuator/mappings', params: [], headers: [], body: '' },
  'act-env':       { title: 'GET /actuator/env', desc: 'Shows every property loaded from application.properties, env vars, system props', analogy: '⚙️ The complete settings panel — every configuration knob, where it came from, and what its value is. Passwords are masked.', concept: '<strong>Property sources in priority order</strong>: command-line args > env vars > application-dev.properties > application.properties > Spring defaults. Sensitive values are masked as <code>******</code>. <strong>Never expose in production.</strong>', method: 'GET', url: '/actuator/env', params: [], headers: [], body: '' },
  'profile-info':  { title: 'GET /api/info', desc: 'Shows which profile is active and what config values were loaded', analogy: '🏢 Asking the building manager which office configuration is active today — dev lab setup or production floor setup.', concept: '<strong>Spring Profiles</strong>: <code>application.properties</code> sets <code>spring.profiles.active=dev</code>, so Spring also loads <code>application-dev.properties</code>. The <code>Environment</code> bean gives you access to all loaded properties at runtime.', method: 'GET', url: '/api/info', params: [], headers: [], body: '' },
  'profile-value': { title: '@Value — Inject any property into a field', desc: 'Demonstrates @Value("${property.key}") reading from application.properties', analogy: '📋 Like a staff handbook. Any employee (@Component) can look up any value from the handbook without hardcoding it.', concept: '<strong>@Value("${spring.application.name}")</strong> reads from whichever properties file was loaded for the current profile and injects it directly into the field. Change the profile → different values → zero code changes.', method: 'GET', url: '/api/info', params: [], headers: [], body: '' },
  'val-allbad':    { title: 'POST /api/students — All Fields Invalid', desc: '@Valid triggers — all 3 fields fail their constraints simultaneously', analogy: '🏦 A loan form where every field is wrong — blank name, fake email, empty course. The receptionist rejects it with a list of exactly what needs fixing.', concept: '<strong>@Valid</strong> activates all annotations on <code>Student</code> fields. Spring\'s validator checks every field before the method body runs. <strong>MethodArgumentNotValidException</strong> carries the full list of all failing fields.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "",\n  "email": "not-an-email",\n  "course": ""\n}' },
  'val-partialbad': { title: 'POST /api/students — Email Invalid Only', desc: 'Only @Email constraint fails — other fields are fine', analogy: '📧 A form where name and course are fine, but the email looks like "dharani@@@@". Only that one field comes back as an error.', concept: '<strong>@Email</strong> validates the format using a regex pattern. Even if name and course pass, Spring rejects the whole request and reports only the failing field.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Dharani",\n  "email": "dharani@@@@invalid",\n  "course": "Java"\n}' },
  'val-toolong':   { title: 'POST /api/students — Name Too Long', desc: '@Size constraint — name exceeds 50 character limit', analogy: '📝 A form field that only allows 50 characters — you typed 80. The system stops you before saving.', concept: '<strong>@Size(min=2, max=50)</strong> enforces length constraints. The message you defined in the annotation is exactly what appears in the error response.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "This name is way too long and exceeds the fifty character maximum limit set",\n  "email": "test@example.com",\n  "course": "Java"\n}' },
  'val-valid':     { title: 'POST /api/students — Valid Request ✓', desc: 'All constraints pass — student is saved to the database', analogy: '✅ A perfectly filled form — name is present, email is valid, course is specified. It sails through validation and gets filed.', concept: 'When all <code>@Valid</code> checks pass, the method body runs normally. Notice how the controller method itself has zero validation logic — it just calls the service. <strong>Validation is declarative.</strong>', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Dharani",\n  "email": "dharani@example.com",\n  "course": "Spring Boot"\n}' },
  'ex-notfound':   { title: 'GET /api/students/999 → 404', desc: 'StudentNotFoundException → @ControllerAdvice → structured 404 JSON', analogy: '🏨 Asking hotel reception for room 999 — it doesn\'t exist. Instead of a crash, you get a polite structured message.', concept: '<strong>Flow:</strong> Controller → Service → <code>findById(999)</code> → empty Optional → <code>.orElseThrow()</code> fires <code>StudentNotFoundException</code> → bubbles up to <strong>@RestControllerAdvice</strong> → returns 404 JSON. The controller itself has zero error-handling code.', method: 'GET', url: '/api/students/999', params: [], headers: [], body: '' },
  'ex-duplicate':  { title: 'POST /api/students → 409 Conflict', desc: 'DuplicateEmailException thrown when same email is registered twice', analogy: '📋 Trying to register for an exam with an email already in the system.', concept: '<strong>@ExceptionHandler(DuplicateEmailException.class)</strong> maps this exception to HTTP <code>409 Conflict</code>. First POST will succeed (201). The second POST with the same email triggers the exception.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Test User",\n  "email": "dharani@example.com",\n  "course": "Java"\n}' },
  'ex-badtype':    { title: 'GET /api/students/abc → 400 Bad Type', desc: 'MethodArgumentTypeMismatchException — "abc" cannot be converted to Long', analogy: '🔢 Asking for employee #"abc" — the ID must be a number.', concept: '<strong>MethodArgumentTypeMismatchException</strong> is thrown automatically when a path variable can\'t be converted to the required type. Without a handler you\'d get a generic 400. With our handler you get a specific message.', method: 'GET', url: '/api/students/abc', params: [], headers: [], body: '' },
  'ex-badjson':    { title: 'POST /api/students → 400 Bad JSON', desc: 'HttpMessageNotReadableException — malformed JSON body', analogy: '📄 Submitting a form that\'s been torn and is unreadable. Spring can\'t parse it.', concept: '<strong>HttpMessageNotReadableException</strong> is thrown by Jackson when the request body is malformed. Our handler converts it to a clean, user-friendly 400 message.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: 'this is { not valid } json!!' },
  'students-getAll':   { title: 'GET /api/students', desc: 'JpaRepository.findAll() — Spring generates SELECT * FROM student', analogy: '🗄️ Opening the filing cabinet and reading every folder. JPA translates this into a full SQL SELECT behind the scenes.', concept: '<strong>JpaRepository</strong>: by extending it, your interface inherits <code>findAll()</code>, <code>save()</code>, <code>findById()</code>, <code>deleteById()</code> for free. Check your terminal — you\'ll see the actual SQL because <code>show-sql=true</code>.', method: 'GET', url: '/api/students', params: [], headers: [], body: '' },
  'students-getOne':   { title: 'GET /api/students/{id}', desc: 'JpaRepository.findById() — SELECT WHERE id = ?', analogy: '📁 Pulling a specific folder from the filing cabinet by its label number.', concept: '<strong>findById(id)</strong> returns an <code>Optional&lt;Student&gt;</code>. If the row exists, you get it wrapped in Optional. If not, Optional.empty() → 404. This pattern avoids null pointer exceptions.', method: 'GET', url: '/api/students/1', params: [], headers: [], body: '' },
  'students-create':   { title: 'POST /api/students', desc: 'JpaRepository.save() — INSERT INTO student ...', analogy: '📝 Creating a new student record and filing it. JPA issues an INSERT and returns the saved row — now with a DB-generated ID.', concept: '<strong>save(entity)</strong> — if the entity has no ID, JPA runs INSERT. If it has an ID, JPA runs UPDATE. The returned object has the auto-generated ID filled in by the DB.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Dharani",\n  "email": "dharani@example.com",\n  "course": "Java"\n}' },
  'students-update':   { title: 'PUT /api/students/{id}', desc: 'findById + save() — JPA runs UPDATE when entity already has an ID', analogy: '✏️ Updating an existing student record. JPA knows it\'s an update because the entity already has an ID.', concept: '<strong>save(existingEntity)</strong> — when the entity\'s ID already exists in the DB, Hibernate issues an UPDATE instead of INSERT. Notice the SQL: <code>update student set ...</code>.', method: 'PUT', url: '/api/students/1', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Dharani Updated",\n  "email": "dharani@example.com",\n  "course": "Spring Boot"\n}' },
  'students-delete':   { title: 'DELETE /api/students/{id}', desc: 'JpaRepository.deleteById() — DELETE FROM student WHERE id = ?', analogy: '🗑️ Shredding a student file permanently from the cabinet.', concept: '<strong>deleteById(id)</strong> runs a DELETE SQL. We first check <code>existsById()</code> to return a proper 404 if the student doesn\'t exist, rather than silently doing nothing.', method: 'DELETE', url: '/api/students/1', params: [], headers: [], body: '' },
  'students-byCourse': { title: 'GET /api/students/course/{course}', desc: 'Derived query — Spring generates SQL just from the method name!', analogy: '🔍 Asking "give me all students enrolled in Java." You wrote no SQL — Spring read the method name <code>findByCourse</code> and wrote the WHERE clause for you.', concept: '<strong>Derived Query Methods</strong>: Spring Data reads the method name <code>findByCourse(String course)</code> and generates <code>SELECT * FROM student WHERE course = ?</code> automatically.', method: 'GET', url: '/api/students/course/Java', params: [], headers: [], body: '' },
  'students-search':   { title: 'GET /api/students/search?keyword=dha', desc: 'findByNameContainingIgnoreCase — LIKE search, derived from method name', analogy: '🔎 A search box that matches any student whose name contains your keyword, case-insensitively.', concept: '<strong>findByNameContainingIgnoreCase(keyword)</strong> → Spring generates: <code>SELECT * FROM student WHERE LOWER(name) LIKE LOWER(\'%keyword%\')</code>. No SQL written.', method: 'GET', url: '/api/students/search', params: [{key:'keyword',value:'dha',enabled:true}], headers: [], body: '' },
  'dto-getall':        { title: 'GET /api/students — Response now includes displayLabel', desc: 'Every student response has a computed displayLabel — "Dharani → Spring Boot" — not in the database', analogy: '🪟 A window vs a door. The @Entity (Student) is your internal room. The DTO (StudentResponseDTO) is the window others look through — you control exactly what they see.', concept: '<strong>Before DTOs</strong>: returning <code>Student</code> directly meant the API shape = DB schema. <strong>After DTOs</strong>: <code>displayLabel</code> in every response is computed in <code>fromEntity()</code> and never touches the DB. The API and DB are now independent.', method: 'GET', url: '/api/students', params: [], headers: [], body: '' },
  'dto-create':        { title: 'POST /api/students — Request has no id field', desc: 'StudentRequestDTO only accepts name, email, course — there is no id to send', analogy: '🏦 Filling in a new bank account form. The bank assigns your account number — you never write one in.', concept: '<strong>Two separate shapes for request vs response</strong>: <code>StudentRequestDTO</code> (what you send) has ONLY <code>name</code>, <code>email</code>, <code>course</code>. Validation annotations (<code>@NotBlank</code>, <code>@Email</code>) now live on the DTO, not the entity.', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Dharani",\n  "email": "dharani.test@example.com",\n  "course": "Spring Boot"\n}' },
  'dto-id-ignored':    { title: 'POST /api/students — Send {"id": 99, ...} and watch it be ignored', desc: 'Even if a malicious client tries to force their own ID, the DTO has no id field so Jackson drops it silently', analogy: '📮 A locked mailbox with a slot just wide enough for letters. You can try to push a brick through — it won\'t fit.', concept: '<strong>Jackson\'s unknown-field behavior</strong>: when deserializing into <code>StudentRequestDTO</code>, Jackson ignores <code>id: 99</code> because there\'s no matching field. Without a DTO, a client sending <code>id: 99</code> could overwrite an existing record! <strong>DTOs are a security layer.</strong>', method: 'POST', url: '/api/students', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "id": 99,\n  "name": "Hacker",\n  "email": "hacker@example.com",\n  "course": "SQL Injection"\n}' },
  'page-default':      { title: 'GET /api/students/paged — Page 0, Size 3 (defaults)', desc: 'Returns first 3 students with total page count, element count, and navigation metadata', analogy: '📖 A book with a table of contents. Instead of handing you the entire 500-page book, the library says: "Here are pages 1–3. There are 8 total pages."', concept: '<strong>Pagination</strong>: instead of <code>SELECT * FROM student</code>, you issue <code>SELECT * FROM student ORDER BY id ASC LIMIT 3 OFFSET 0</code>. The <code>Page&lt;Student&gt;</code> response includes: <code>content</code>, <code>totalElements</code>, <code>totalPages</code>, <code>number</code>, <code>first</code>/<code>last</code>.', method: 'GET', url: '/api/students/paged', params: [{key:'page',value:'0',enabled:true},{key:'size',value:'3',enabled:true}], headers: [], body: '' },
  'page-2':            { title: 'GET /api/students/paged — Page 1, Size 2 (second page)', desc: 'Moves to page index 1 — skips the first 2 students, returns the next 2', analogy: '📖 Asking for chapter 2 of the book. Chapter 1 had pages 1 and 2. Chapter 2 starts at page 3.', concept: '<strong>Page numbering is 0-indexed</strong>: page=0 is first, page=1 is second. With size=2: page 0 → students 1,2 | page 1 → students 3,4. <strong>SQL</strong>: <code>SELECT * FROM student LIMIT 2 OFFSET 2</code>.', method: 'GET', url: '/api/students/paged', params: [{key:'page',value:'1',enabled:true},{key:'size',value:'2',enabled:true}], headers: [], body: '' },
  'page-sortname':     { title: 'GET /api/students/paged — Sorted by name A→Z', desc: 'Same pagination but students are returned in alphabetical name order', analogy: '📚 Asking the librarian: "Give me the first 3 books, alphabetically by title."', concept: '<strong>Sort.by("name").ascending()</strong> tells Hibernate to add <code>ORDER BY name ASC</code> to the SQL. The sort column must match the Java field name. You can also sort by multiple columns.', method: 'GET', url: '/api/students/paged', params: [{key:'page',value:'0',enabled:true},{key:'size',value:'3',enabled:true},{key:'sortBy',value:'name',enabled:true},{key:'direction',value:'asc',enabled:true}], headers: [], body: '' },
  'page-sortcourse':   { title: 'GET /api/students/paged — Sorted by course Z→A', desc: 'Students sorted by course name in descending order', analogy: '📚 Asking for books sorted by genre in reverse alphabetical order.', concept: '<strong>Sort.by("course").descending()</strong> → <code>ORDER BY course DESC</code>. Sorting, filtering, and pagination all compose — you can add <code>findByCourse(String, Pageable)</code> and get filtered + sorted + paginated in one call.', method: 'GET', url: '/api/students/paged', params: [{key:'page',value:'0',enabled:true},{key:'size',value:'5',enabled:true},{key:'sortBy',value:'course',enabled:true},{key:'direction',value:'desc',enabled:true}], headers: [], body: '' },
  'tx-count':          { title: 'GET /api/students/count', desc: 'Returns the total number of students — use before and after the rollback demo', analogy: '🧮 Counting items in your shopping cart before and after a failed transaction. If a rollback worked, the count stays the same.', concept: '<strong>@Transactional(readOnly = true)</strong>: tells Hibernate "this transaction will only read, never write." Hibernate skips the expensive dirty-checking phase. Use this counter endpoint BEFORE and AFTER the rollback demo to prove the DB was not modified.', method: 'GET', url: '/api/students/count', params: [], headers: [], body: '' },
  'tx-bulk-success':   { title: 'POST /api/students/bulk — Bulk Enroll (All Succeed)', desc: 'Enroll 3 students atomically — all 3 are saved in one transaction, count goes up by 3', analogy: '🏦 A bank transfer that moves money from 3 accounts simultaneously. Either all 3 complete, or none do.', concept: '<strong>@Transactional</strong>: Spring wraps <code>enrollBulk()</code> in a single DB transaction. Spring creates a JDBC connection, calls <code>BEGIN TRANSACTION</code>, runs all INSERTs, then <code>COMMIT</code>. All 3 students appear at the same instant.', method: 'POST', url: '/api/students/bulk', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '[\n  {"name":"Alice","email":"alice@example.com","course":"Spring Boot"},\n  {"name":"Bob","email":"bob@example.com","course":"Java"},\n  {"name":"Carol","email":"carol@example.com","course":"React"}\n]' },
  'tx-bulk-rollback':  { title: 'POST /api/students/bulk — Bulk Enroll (Rollback Demo)', desc: 'The 3rd student has a duplicate email — @Transactional rolls back the first 2 saves automatically', analogy: '🏦 Same bank transfer, but the 3rd account is frozen. The bank cancels ALL transfers. You either get the full result or nothing.', concept: '<strong>Rollback in action:</strong> The loop saves student 1 ✓ and student 2 ✓, then hits student 3 with a duplicate email → <code>DuplicateEmailException</code> → <code>@Transactional</code> catches the unchecked exception → ROLLBACK → all 3 INSERTs are undone. Check <code>/count</code> after — it should be UNCHANGED.', method: 'POST', url: '/api/students/bulk', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '[\n  {"name":"Dave","email":"dave@example.com","course":"Spring Boot"},\n  {"name":"Eve","email":"eve@example.com","course":"Java"},\n  {"name":"Dharani Duplicate","email":"dharani@example.com","course":"React"}\n]' },
  'orders-summary':    { title: 'GET /api/orders/summary', desc: 'OrderService uses injected ProductService to combine data from both services', analogy: '🏪 The store manager asks the inventory system (ProductService) and the billing system (OrderService) for data — wired together by Spring.', concept: '<strong>Dependency Injection</strong>: <code>OrderService</code> declares it needs a <code>ProductService</code> in its constructor. Spring finds the <code>ProductService</code> bean it already created and passes it in. Both services share the <strong>same single instance</strong>.', method: 'GET', url: '/api/orders/summary', params: [], headers: [], body: '' },
  'orders-getAll':     { title: 'GET /api/orders', desc: 'List all placed orders — empty until you POST one', analogy: '📋 Checking the order ledger. Start empty, then place some orders with POST and watch this list grow.', concept: '<strong>Spring Singleton Scope</strong>: The <code>OrderService</code> bean is created once and reused. The <code>orders</code> list inside it persists across all requests for the lifetime of the server.', method: 'GET', url: '/api/orders', params: [], headers: [], body: '' },
  'orders-place':      { title: 'POST /api/orders', desc: 'Place an order — OrderService calls ProductService internally', analogy: '🛒 Placing an order at the counter. The cashier (OrderService) looks up the product price from the catalog (ProductService).', concept: '<strong>The DI chain in action</strong>: <code>OrderController</code> gets <code>OrderService</code> injected. <code>OrderService</code> gets <code>ProductService</code> injected. When you call this endpoint, <code>OrderService.placeOrder()</code> internally calls <code>productService.getProductById()</code>. Try productId 1, 2, or 3.', method: 'POST', url: '/api/orders', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "productId": 1,\n  "quantity": 2\n}' },
  'products-getAll':   { title: 'GET /api/products', desc: 'Returns all products as a JSON array — the full list', analogy: '🏬 Walking into a shop and saying "show me everything you have."', concept: '<strong>@RestController</strong> automatically converts the Java <code>List&lt;Product&gt;</code> return value to a JSON array using Jackson. No extra code needed — Spring handles it.', method: 'GET', url: '/api/products', params: [], headers: [], body: '' },
  'products-getOne':   { title: 'GET /api/products/{id}', desc: 'Fetch a single product by ID — returns 200 or 404', analogy: '🔍 Asking a shopkeeper "do you have item #2?" — they check and either hand it over (200) or say "sorry, not found" (404).', concept: '<strong>ResponseEntity</strong> lets you control the HTTP status code. <code>Optional</code> from the service maps to either <code>200 OK</code> with the product body, or <code>404 Not Found</code>.', method: 'GET', url: '/api/products/1', params: [], headers: [], body: '' },
  'products-create':   { title: 'POST /api/products', desc: '@RequestBody — Spring deserializes incoming JSON into a Java object', analogy: '📦 Supplier delivering a new product to the shop. You send the product details, Spring unpacks them into a Product object.', concept: '<strong>@RequestBody</strong> tells Spring to read the JSON body and convert it into a <code>Product</code> Java object automatically. The response is <code>201 Created</code> with the saved product (now with an ID).', method: 'POST', url: '/api/products', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Tablet",\n  "price": 45000\n}' },
  'products-update':   { title: 'PUT /api/products/{id}', desc: 'Update an existing product — full replacement of name and price', analogy: '✏️ Editing an existing product listing. You send the new details, Spring finds the product by ID, updates it in place.', concept: '<strong>@PutMapping</strong> handles HTTP PUT — conventionally used for full updates. The service finds the item, replaces its fields, and returns <code>200 OK</code>. If the ID doesn\'t exist, <code>404 Not Found</code>.', method: 'PUT', url: '/api/products/1', params: [], headers: [{key:'Content-Type',value:'application/json',enabled:true}], body: '{\n  "name": "Gaming Laptop",\n  "price": 120000\n}' },
  'products-delete':   { title: 'DELETE /api/products/{id}', desc: 'Remove a product — returns 204 No Content on success', analogy: '🗑️ Removing a product from the shelf. A successful delete returns 204 (done, nothing to show).', concept: '<strong>@DeleteMapping</strong> handles HTTP DELETE. The convention is to return <code>204 No Content</code> on success. <code>Void</code> as the type signals no response body.', method: 'DELETE', url: '/api/products/3', params: [], headers: [], body: '' },
}
