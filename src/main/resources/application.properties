# ── Active Profile ───────────────────────────────────────────
# This is the ONLY thing you change to switch environments.
# Spring will ALSO load application-{profile}.properties on top of this file.
spring.profiles.active=dev

# ── Common config (all environments) ─────────────────────────
server.port=8080
spring.application.name=spring-boot-learner
